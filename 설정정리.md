# 대여약정서 서비스 (내지마요) — 설정 정리

> 최초 작성: 2026-05-27 / 최종 업데이트: 2026-07-04 (버그수정 + 보안 대수술 배포)

## ★ 2026-07-04 — 세무상담 버그수정 + 보안 대수술 (배포 완료, 라이브 검증)

전수 클릭테스트(Playwright) + Fable 보안감사 후 위험 전량 수정. 배포 `vercel --prod` (dpl_DeNDRVRDe3P3o6z2g879rBRFnxp3, naejimayo.com), 빌드 OK, 라이브 실측 검증.

**버그 수정**
- 세무상담 폼 제출 500 → `tax_consultations.email NOT NULL`인데 폼이 이메일 미수집 → null 삽입 위반. 수정: Supabase에서 `ALTER TABLE tax_consultations ALTER COLUMN email DROP NOT NULL;` 실행 → 재제출 200 "신청이 완료되었습니다" 확인.

**보안 수정 (Critical/High/Med 전량)**
- [Critical] 결제 금액 위변조: `payment/confirm`이 클라 amount 무시하고 서버 `order.amount`로 토스 승인 + 응답 `totalAmount/orderId` 재대조. (결제창 미연동이라 선반영)
- [Critical] PDF 헤더 우회: `pdf/route.ts`의 `x-admin-access:1` 삭제 + `middleware.ts`(신규)가 `/api/*`에서 해당 헤더 strip. 라이브 403 확인.
- [High] 약정서 조회 IDOR: `GET /api/agreements/[id]`를 admin쿠키 OR `?token`(서명토큰) 없으면 `toPublicView`(민감정보 제외)만 반환. 라이브: 무인증=공개필드만(이메일/토큰/서명/order 유출 0), 토큰=전체.
  - 배선: `create`가 `lenderToken` 반환 → `form-store`(saveLenderToken/loadLenderToken, sessionStorage) → Step3 저장 / Step4 sign-lender·Step5 request-borrower에 token 전달 / Step6 → `/complete?t=token` → complete page searchParams → `CompleteView`가 token으로 fetch + PDF/audit/subscribe/transfer 링크. `clearForm`에 토큰키 추가.
- [High] 미들웨어 부재 → `middleware.ts` 신규.
- [Med-High] `sign-lender`: `lenderSignToken` 검증 추가(차용자와 대칭) + 재서명 차단.
- [Med] `request-borrower`(lenderToken 필수 + 레이트리밋), `otp/send`(재발송 30초 쿨다운 + 약정서당 10분 5회 + IP 10분 15회), admin/advisor login(IP 10분 10회), `getClientIp`(req.ip>x-real-ip>xff 마지막). 공용 `lib/rate-limit.ts` 신규.
- XSS 스윕: `dangerouslySetInnerHTML`은 MetaPixel(정적 FB픽셀 스크립트, env값)뿐 → 안전.

**남은 것(다음 주, 결제와 함께)**
- 토스 결제창(위젯 SDK) 실연동 + 프로덕 `NEXT_PUBLIC_TOSS_CLIENT_KEY`/`TOSS_SECRET_KEY`. 토스 가입비 22만(최초1회)+연관리비 11만, 카드 3.4%(+VAT), 월고정비 없음.
- (개선·비필수) admin 세션토큰=비번해시(개별 무효화 불가) → JWT 권장 / 인메모리 레이트리밋 → 서버리스 멀티인스턴스 대비 Upstash 등 공유저장소 권장.

---

## ★ 2026-07-03 — 서명/도장 입력 확장 (배포 완료)
- 서명 입력을 **탭 3개**로: ① **직접 그리기**(기존 react-signature-canvas 유지) ② **이름으로 만들기**(이름 입력 → 흘림체·붓글씨·기울임·정자체 4종 서명 자동생성 → 선택) ③ **도장**(이름 → 붉은 인영 6종[원형·이중/낙관/굵은선, 사각·전각/음각, 타원] 갤러리 선택 or 이미지 업로드)
- 신규: `components/StampMaker.tsx`, `components/SignatureStampField.tsx` (순수 canvas 자체 구현, brand 파랑 적용). hwaseon-erp 전자계약의 도장/서명 로직을 이식.
- 교체: `components/create/Step4LenderSign.tsx`(대여자)·`components/sign/BorrowerSign.tsx`(차용자)의 `SignatureCanvasField` → `SignatureStampField`. 결과는 동일하게 PNG dataURL 을 onChange 로 전달(서버 서명 임베드 흐름 변경 없음).
- 검증: tsc 0 / next build 성공 / 실브라우저 렌더(도장 6종·서명 4종) 확인. 
- 본인확인은 기존 **이메일 OTP(6자리)** 유지. (원하면 hwaseon처럼 '받은 이메일 주소 재입력'으로 전환 가능 — 모바일에서 코드 확인하러 앱 전환/뒤로가기 번거로움이 사라짐.)

---

---

## 서비스 정보

- **서비스명**: / 내지마요
- **URL**: https://naejimayo.com
- **GitHub**: https://github.com/HWASEON-AD/loan-agreement-service
- **관리자 대시보드**: https://naejimayo.com/admin/dashboard (비밀번호: hwaseon@00)
- **세무상담 어드민**: https://naejimayo.com/admin/consult (비밀번호: hwaseon@00)

---

## 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 14.2.35 (App Router) + TypeScript |
| 스타일 | Tailwind CSS 3.4 |
| DB | Supabase (PostgreSQL) — Mock 모드 시 메모리 사용 |
| PDF 생성 | pdf-lib + @pdf-lib/fontkit (한글 NotoSansKR.otf 임베드) |
| 서명 캔버스 | react-signature-canvas |
| 이메일 | Resend (Mock 모드 시 console.log) |
| 결제 | 토스페이먼츠 (Mock 모드 시 자동 성공) |
| 배포 | Vercel (hwaseon-ads-projects) |

---

## 서비스 개요

- **서비스**: 가족 간 금전 대여약정서 자동화 + 전자서명 + 우체국 내용증명 발송
- **요금**: 30,000원/건
- **이자율**: 무이자 한도 217,390,000원 이하 0%, 초과 시 4.6% 자동 적용

---

## 주요 기능 (2026-05-28 기준 전체 완성)

1. 금액 입력 + 무이자 한도(2억 1,739만원) + 이자 자동 계산
2. 당사자 정보 입력 (생년월일 6자리만, 주민번호 수집 금지)
3. 약정서 미리보기 — 법적 문서 스타일 (제목 중앙정렬, 당사자 정보박스, 조항 본문, 서명란)
4. 대여자 이메일 OTP + 서명 캔버스
5. 차용자 서명 요청 링크 발송 (7일 유효)
6. 차용자 이메일 OTP + 서명 캔버스
7. 결제 (30,000원, 토스페이먼츠)
8. 서명된 약정서 PDF 생성 (SHA-256 해시)
9. **전자서명 감사추적인증서 PDF 자동발급** ← 2026-05-28 신규 추가
   - 전자문서법 제4조, 전자서명법 제3조 기반
   - 서명자별 IP/기기/시각/OTP여부/문서해시/서명이미지 포함
   - 민사소송법 서명진정성립 추정 원칙 면책 문구 포함
10. 관리자 대시보드 개선 `/admin/dashboard` — 통계카드4종 / 필터탭 / 상세모달 / 모바일반응형
11. OTP 버튼 1열 세로 배치 (인증 확인 > 재발송 순서)
12. **자금조달계획서 AI 자동작성** `/funding-plan` ← 2026-05-28 신규
    - 3단계 위자드 (기본정보 → 자연어 스토리 → 검토+다운로드)
    - Claude API(claude-sonnet-4-6) 자연어→항목 자동 분류
    - 주택/토지 서식 PDF 생성 (NotoSansKR 한글 폰트)
    - Mock 모드: ANTHROPIC_API_KEY 없이 전체 플로우 동작
13. **상단 고정 네비게이션** `components/SiteHeader.tsx` ← 2026-05-28 신규
    - fixed 포지션 (오버스크롤 시 사라짐 방지)
    - 탭: 약정서 작성 / 자금조달계획서 AI 자동작성 / 세무상담
    - 로고: 파란 사각형 + 흰색 슬래시(/)
    - 파비콘: app/icon.svg
14. **세무상담 섹션** `components/TaxConsultSection.tsx` ← 2026-05-28 신규
    - 전직 용산세무서장 출신 약력 (대통령실 파견, 중부청 조사1국 등)
    - 사진 자리 확보 (img 태그 교체 대기)
    - 카카오 채널 상담 버튼 (링크 미확정)
15. **Meta Pixel + OG 태그** `components/MetaPixel.tsx` ← 2026-05-28 신규
    - PageView / InitiateCheckout / Purchase 이벤트
    - OG 태그 완비 (Facebook/Instagram 광고 대응)
    - 환경변수: NEXT_PUBLIC_META_PIXEL_ID

---

## API 목록

| 메서드 | 경로 | 기능 |
|---|---|---|
| POST | /api/agreements/create | 약정서 생성 |
| GET | /api/agreements/[id] | 약정서 + 주문 조회 |
| GET | /api/agreements/by-token/[token] | 차용자 토큰으로 조회 |
| POST | /api/otp/send | OTP 이메일 발송 |
| POST | /api/otp/verify | OTP 검증 |
| POST | /api/agreements/[id]/sign-lender | 대여자 서명 |
| POST | /api/agreements/[id]/request-borrower | 차용자 서명 요청 이메일 |
| POST | /api/agreements/[id]/sign-borrower | 차용자 서명 (재서명 차단, OTP 서버검증) |
| POST | /api/payment/confirm | 결제 확인 |
| GET | /api/agreements/[id]/pdf?token=xxx | 서명된 약정서 PDF 다운로드 |
| GET | /api/agreements/[id]/audit-cert?token=xxx | **감사추적인증서 PDF 다운로드** |
| GET | /api/admin/orders | 관리자 주문 목록 |
| GET | /api/admin/orders/csv | 내용증명 CSV |
| PUT | /api/admin/orders/[id]/mark-sent | 발송 완료 마킹 |
| PUT | /api/admin/orders/[id]/status | 주문 상태 변경 |
| POST | /api/admin/login | 관리자 로그인 |
| POST | /api/admin/logout | 관리자 로그아웃 |

---

## Supabase 설정

- **프로젝트 URL**: https://kepzsboxjulzygehmzpf.supabase.co
- **Anon Key**: (환경변수 참고)
- **Service Role Key**: (환경변수 참고)
- **테이블**: agreements, orders, otp_codes, signature_records
- **RLS**: 전 테이블 활성화, anon 접근 차단

---

## 환경변수 (.env.local / Vercel)

```
NEXT_PUBLIC_BASE_URL=https://loan-agreement-service.vercel.app
NEXT_PUBLIC_MOCK_MODE=false

NEXT_PUBLIC_SUPABASE_URL=https://kepzsboxjulzygehmzpf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=← Vercel 환경변수 확인
SUPABASE_SERVICE_ROLE_KEY=← Vercel 환경변수 확인

RESEND_API_KEY=                    ← 발급 필요
SMTP_HOST=smtp.worksmobile.com
SMTP_PORT=587
SMTP_USER=gt.min@hwaseon.com
SMTP_PASS=4pZKZImiP1aD

NEXT_PUBLIC_TOSS_CLIENT_KEY=       ← 발급 필요
TOSS_SECRET_KEY=                   ← 발급 필요

ADMIN_PASSWORD=admin1234           ← 배포 전 변경 권고

ANTHROPIC_API_KEY=                 ← 자금조달계획서 AI 실모드 필요
NEXT_PUBLIC_META_PIXEL_ID=         ← Meta 광고 픽셀 ID (광고 시작 전 등록)
```

---

## 실행 방법

```
cd "C:\Users\gtmin\Dropbox\5.개발\loan-agreement-service"
npm install
npm run dev
→ http://localhost:3000
```

Mock 모드 (외부 API 없이): `.env.local` 에서 `NEXT_PUBLIC_MOCK_MODE=true`

---

## 2026-06-16 업데이트 내역

### 신규 기능
- `/admin/consult` — 세무상담 전용 어드민 게시판 (목록/상세/상태변경: 대기→처리완료)
- `DELETE /api/admin/agreements/[id]` — 약정서 삭제 API 신규
- 어드민 대시보드 체크박스 전체선택 + PDF 일괄 다운로드 + 일괄 삭제
- `public/og-image.jpg` — 카카오 미리보기 이미지 교체

### 버그 수정
- PDF 어드민 다운로드 403 버그: `<a>` 태그에서 커스텀 헤더 못 보내는 문제
  → `app/api/agreements/[id]/pdf/route.ts`에 `isAdminAuthenticated()` 쿠키 체크 추가

### 변경
- 어드민 대시보드 통계카드 4개→3개 (총 대여금액 제거)
- `BASE_URL` `loan-agreement-service.vercel.app` → `naejimayo.com`
- 테스트용 인코딩 깨진 데이터 2건 Supabase에서 삭제

---

## 빌드 결과 (2026-06-01 최초 / 2026-06-16 기준)

- 빌드: 통과 (npm run build)
- 페이지 수: 27개
- TypeScript 에러: 0
- 최신 커밋: fix: 자금조달계획서 PDF 깨짐 수정 — 원본 정부 양식 템플릿 오버레이 방식

### 자금조달계획서 PDF 방식 (2026-06-01 Vercel 완전 해결)
- **주택 서식**: `public/forms/housing-form.pdf` (공식 원본) 위에 데이터 오버레이
- 좌표: pdfjs-dist로 실제 원본 텍스트 위치 추출 → 정밀 배치
- **토지 서식**: pdf-lib 직접 생성 (NotoSansKR 폰트 subset:false)
- 한글 폰트: NotoSansKR-Regular.otf, subset:false
- **Vercel 핵심 이슈**: `public/` 파일에 `fs.readFile` 접근 불가
  - 해결: `loadFont()` / `loadTemplate()` 에 HTTP fallback 추가
  - fs 실패 → `${NEXT_PUBLIC_BASE_URL}/fonts/NotoSansKR-Regular.otf` CDN fetch
  - 코드 위치: `lib/funding-pdf.ts`, `lib/pdf-generator.ts`
- **배포 이슈**: Vercel 큐 stuck → `vercel rm <deployment-url>` 으로 해제
- 최종 확인: PDF 3.77MB, 한글 완전 정상 (2026-06-01 Vercel 테스트 통과)

## 남은 작업 체크리스트
- [ ] OG 이미지 제작 업로드 `public/og-image.png` (1200×630px)
- [ ] Meta Pixel ID 발급 → Vercel `NEXT_PUBLIC_META_PIXEL_ID` 등록
- [ ] 세무상담 카카오 채널 실제 링크로 교체 (`TaxConsultSection.tsx` href)
- [ ] 세무사 사진 파일 받아서 `TaxConsultSection.tsx` img 태그 교체
- [ ] Resend API키 발급
- [ ] 토스페이먼츠 가맹점 등록
- [ ] 도메인 연결
- [ ] ADMIN_PASSWORD 변경

---

## 법적 제약 (코드에 반영됨)

- 주민번호 수집 금지 → 생년월일 6자리 input (maxLength=6)
- "절세"/"증여세" 문구 없음
- 확정일자 대행 없음 → 인터넷등기소(iros.go.kr) 안내 텍스트만
- 결제 명목: "대여약정서 작성 및 내용증명 발송 서비스" 고정
- 면책문구: 약관 "법률 서비스 아님" + 감사추적인증서 내 면책 문구 포함
- 감사로그: IP/UA/OTP여부/SHA-256 해시 → Supabase signature_records 테이블

---

## 전자서명 법적 근거 (2026-05-28 법적검토 완료)

- **전자서명법 제3조**: 이메일 OTP 기반 전자서명 = 일반 전자서명으로 법적 유효
- **민법 낙성불요식 원칙**: 당사자 합의 있으면 형식 불문 계약 성립
- **입증책임 역전**: 서명 부인하는 쪽이 직접 증명해야 함 (플랫폼 면책)
- **플랫폼 면책**: 법무사가 신분증 확인 후 공증한 것처럼, 정상 절차(OTP+감사로그) 이행 시 플랫폼 책임 없음
- **감사추적인증서**: 모두싸인과 동일 수준의 전자 증거물 자동발급 구현 완료

---

## 남은 작업 (선택사항)

- [ ] Resend API 키 발급 → 실제 이메일 발송 활성화
- [ ] 토스페이먼츠 가맹점 등록 → 실제 결제 활성화
- [ ] ADMIN_PASSWORD 강력한 비밀번호로 변경
- [ ] 도메인 연결 (현재 vercel.app 서브도메인)
- [ ] AATL 인증서 구매 ($300~1000/년) → Adobe 자물쇠 아이콘 추가 (선택)

---

## 계약갱신 서식 2차 — 표 서식 + 갱신 확인서 (2026-08-15)

브랜치 `feature/renewal-landlord` 커밋 `5ca2a96` (push 완료). **main `55dfaa1` 무변경 = 프로덕션 그대로.**
롤백 지점 = `main 55dfaa1`.

### 무엇이 바뀌었나
- 서식을 **긴 문자열 → 구조체(`FormDoc`)** 로 바꾸고, 화면 미리보기와 PDF가 **같은 구조체**를 렌더링한다.
  표를 공백으로 흉내내지 않으므로 값이 길어져도 칸이 밀리지 않는다.
- 신규 서식 **「주택임대차계약 갱신 확인서」** (임대인·임차인 **양 당사자 서명**) 추가.
- 상단 네비 라벨 `계약갱신 요구 통지서` → **`계약갱신 서식`** (서식 2종). 진입점은 `/renewal` 한 곳에 탭으로 통합.
- 약관 제7조 ① 범위 확대 + **⑤ 신설** (확인서 성격 · 비판정 원칙).

| 파일 | 내용 |
|---|---|
| `lib/renewal-doc.ts` (신규) | 서식 구조 모델 + 통지서/확인서 빌더 + 평문 렌더러 |
| `lib/renewal-pdf.ts` | 표 렌더러로 전면 교체 (셀 줄바꿈·행높이 자동·페이지 분할) |
| `components/renewal/FormDocument.tsx` (신규) | 같은 구조체의 HTML 렌더러 (미리보기·인쇄) |
| `components/renewal/RenewalForm.tsx` | 서식 종류 선택 + 확인서 입력 필드 |
| `app/api/renewal/pdf/route.ts` | 입력을 `doc: FormDoc` 구조체로 변경 (+구조 검증·20KB 상한) |
| `app/globals.css` | `/renewal` 인쇄용 `@page A4` |

### 🚨 반드시 지킬 것 (법률검토 결과)
1. **임대인용 "갱신 통지서"를 만들지 말 것.** 제6조의3의 갱신요구권은 **임차인의 형성권**이라,
   임대인 명의로 "갱신을 요구합니다"라고 쓰면 법적으로 무의미하고 문서 신빙성만 깎인다.
   확인서는 반드시 **양 당사자 서명** 구조로 유지한다.
2. 확인서 문안은 **과거 사실의 확인**이지 **간주 합의가 아니다.** (실제 갱신요구가 없었는데
   "행사한 것으로 본다"는 서명을 받으면 법 제10조 편면적 강행규정으로 무효가 될 소지)
3. **"갱신요구권이 소진되었습니다" 를 서비스가 자동 삽입하지 말 것.**
4. 갱신 요구가 기간 내였는지, 증액이 제7조 범위인지 **판정하지 말 것.**
5. **갱신거절 통지서 계열은 만들지 말 것** (9개 사유 선택 UI 자체가 자가진단이 된다).
6. 기존 안전선 전부 승계: LLM 호출 0회 / 무료 / DB 미저장 / 변호사 연결 없음.

### 🚨 서식 치수를 건드릴 때
`lib/renewal-pdf.ts` 상단 상수. **확인서가 제일 빡빡하다**(표 3개 + 본문 + 참고 + 서명 2줄).
치수를 키우면 A4 한 장을 넘겨 **둘째 장에 서명란만 남는다**. 수정 후 반드시 확인서를 다시 렌더해 확인할 것.

### 검증 (실행함)
- `tsc --noEmit` / `next build` 통과
- PDF 4종(통지서·확인서 × 월세·전세+긴주소) 전부 **A4 210×297mm 1페이지**, 한글 정상
- Playwright 로 양쪽 모드 화면 실측 → 미리보기가 PDF와 동일 구조
- ⚠️ 이메일 발송은 실측하지 않음(실제 메일이 나가므로)
