# 대여약정서 서비스 — 설정 정리

> 작성일: 2026-05-27

---

## 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 14.2.35 (App Router) + TypeScript |
| 스타일 | Tailwind CSS 3.4 |
| DB | Supabase (PostgreSQL) — Mock 모드 시 메모리 사용 |
| 파일 저장 | Supabase Storage |
| PDF 생성 | pdf-lib + @pdf-lib/fontkit |
| 서명 캔버스 | react-signature-canvas |
| 이메일 | Resend (Mock 모드 시 console.log) |
| 결제 | 토스페이먼츠 (Mock 모드 시 자동 성공) |
| 배포 | Vercel |

---

## 서비스 개요

- **서비스**: 가족 간 금전 대여약정서 자동화 + 전자서명 + 우체국 내용증명 발송
- **요금**: 30,000원/건
- **내용증명 실비**: ~12,000~15,000원 (순마진 ~17,000원)
- **법적 근거**: 전자서명법 제3조, 대법원 2026.2 로폼 판결

---

## 주요 기능

1. 금액 입력 + 무이자 한도(2억 1,739만원) + 이자 자동 계산
2. 당사자 정보 입력 (생년월일 6자리만, 주민번호 수집 금지)
3. 약정서 미리보기 (표준 양식 자동 완성)
4. 대여자 이메일 OTP + 서명 캔버스
5. 차용자 서명 요청 링크 발송 (7일 유효)
6. 차용자 이메일 OTP + 서명 캔버스
7. 결제 (30,000원, 토스페이먼츠)
8. PDF 생성 (감사로그 페이지 포함, SHA-256 해시)
9. 관리자 대시보드 (주문 관리, CSV 다운로드, 날짜 필터, 상세 accordion)

---

## API 목록

| 메서드 | 경로 | 기능 |
|---|---|---|
| POST | /api/agreements/create | 약정서 생성 |
| POST | /api/otp/send | OTP 이메일 발송 |
| POST | /api/otp/verify | OTP 검증 |
| POST | /api/agreements/[id]/sign-lender | 대여자 서명 |
| POST | /api/agreements/[id]/request-borrower | 차용자 서명 요청 |
| POST | /api/agreements/[id]/sign-borrower | 차용자 서명 (재서명 차단) |
| POST | /api/payment/confirm | 결제 확인 |
| GET | /api/admin/orders | 관리자 주문 목록 |
| GET | /api/admin/orders/csv | 내용증명 CSV 다운로드 |
| PUT | /api/admin/orders/[id]/mark-sent | 발송 완료 마킹 |
| GET | /api/agreements/by-token/[token] | 차용자 토큰 조회 |

---

## 환경변수 (.env.local)

```
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_MOCK_MODE=true         ← false로 바꾸면 실제 API 사용

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

RESEND_API_KEY=

NEXT_PUBLIC_TOSS_CLIENT_KEY=
TOSS_SECRET_KEY=

ADMIN_PASSWORD=admin1234           ← 배포 전 반드시 변경
```

---

## 실행 방법

```
cd "C:\Users\gtmin\Dropbox\5.개발\loan-agreement-service"
npm install
npm run dev
→ http://localhost:3000
```

관리자: http://localhost:3000/admin (비밀번호: admin1234)

---

## 빌드 결과

- 빌드: 통과 (npm run build)
- 페이지 수: 23개
- TypeScript 에러: 0
- BUILD_ID: rTpX0iQ6Xe507zUWGUWqO

---

## 법적 제약 (코드에 반영됨)

- 주민번호 수집 금지 → 생년월일 6자리 input (maxLength=6)
- "절세"/"증여세" 문구 없음 → 전체 코드 grep 확인
- 도장 날인 없음 → 전자서명 캔버스만 사용
- 확정일자 대행 없음 → 인터넷등기소 안내 텍스트만
- 결제 명목: "대여약정서 작성 및 내용증명 발송 서비스" 고정
- 면책문구: Footer에 "본 서비스는 법률 서비스가 아닙니다"
- 감사로그: 서명 시 IP/UA/OTP여부/SHA-256 해시 저장

---

## 배포 체크리스트

- [ ] Supabase 프로젝트 + DB 스키마 생성 (plans/plan.md 참고)
- [ ] Resend API 키 발급 (resend.com)
- [ ] 토스페이먼츠 가맹점 등록
- [ ] NEXT_PUBLIC_MOCK_MODE=false 변경
- [ ] ADMIN_PASSWORD 강력한 비밀번호로 변경
- [ ] Vercel 배포 + 도메인 연결
